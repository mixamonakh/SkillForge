import { AssessmentCatalog } from '@/features/assessment/assessment-catalog';

export const metadata = { title: 'Диагностика' };

export default function AssessmentPage() {
  return <AssessmentCatalog />;
}
