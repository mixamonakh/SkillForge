import { ImportExportCenter } from '@/features/import-export/import-export-center';

export const metadata = { title: 'Import / Export' };

export default function ImportExportPage() {
  return <ImportExportCenter />;
}
